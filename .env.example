# Rehearsal Calendar Native App - Environment Variables

# API Configuration
# Override the API URL (optional)
# If not set, the app will auto-detect in development or use production URL
#
# Examples:
# - For local development with custom IP: EXPO_PUBLIC_API_URL=http://192.168.1.100:3001/api
# - For staging environment: EXPO_PUBLIC_API_URL=https://staging-api.example.com/api
# - For production build: EXPO_PUBLIC_API_URL=https://api.example.com/api
#
# EXPO_PUBLIC_API_URL=

# Note: EXPO_PUBLIC_ prefix is required for Expo to expose the variable to the app
# Variables without this prefix are only available during build time
