# I — сквозная безопасность, секреты, опасные вызовы и документация

2026-09-11, HEAD25475e8. Только read/search/git metadata; исходники, конфигурация, БД, сервисы не менялись и не запускались. Поиск не является полным чтением каждого найденного файла; coverage разделён ниже.

## IS01 — Medium — secret scanner использует неверный диалект regex и пропускает секреты

`rehearsal-calendar-native/scripts/check-secrets.sh:29-35` содержит шаблоны `JWT_SECRET=[a-zA-Z0-9\-_]{20,}`, `sk-[a-zA-Z0-9]{20,}`, `password=.{8,}`. Но :53 запускает `xargs grep -i -n "$PATTERN"` без -E. В basic regex неэкранированные `{20,}`/`{8,}` не являются указанным квантификатором: обычный токен из20+символов эти правила не обнаруживают. Ошибки grep скрываются `2>/dev/null || true`; :68-69 целиком исключает Markdown. Поэтому новый секрет в обычном code file может пройти check:secrets, хотя это объявлено защитой перед commit/check.

Дополнительно :43 задаёт shell-строку `JWT_SECRET=.*\\|\\|`, которая после shell decoding попадает в basic grep как `JWT_SECRET=.*\|\|`. В GNU grep это альтернативы с пустой веткой, принимающие любую строку; такой allowlist способен выкинуть все найденные совпадения :75-77. На других grep поведение этой конструкции непереносимо; не заявляется проверенный запуск именно macOS grep. Основной дефект квантификаторов не зависит от этой дополнительной ветки. Семантика basic/extended и пустых alternatives сверена с [официальным руководством GNU grep](https://www.gnu.org/software/grep/manual/grep.html), 2026-09-11; fetch одной anchored страницы дал timeout, основной manual найден поиском. Никакой scanner/синтетический test не запускался.

Минимум: явный -E для основных regex, исправленный allowlist с literal `\|\|` в ERE/точным контекстом, корректная обработка ошибок и null-delimited список файлов, не исключать всеmd (2–4ч; + делает существующие правила работоспособными, − coverage набором шаблонов ограничен). Правильно: специализированный scanner с redact и точечными baseline исключениями, pre-commit/CI проверка и отдельные fixtures на known fake secrets/no-secrets/ошибку чтения (0.5–1день; + поддерживаемые правила и проверяемая защита, − настройка baseline и сопровождение). Найденная неисправность не доказывает, что live secret уже опубликован.

## IS02 — Medium, зависит от deployment — rate limits не общие для нескольких процессов

`rehearsal-calendar-native/server/server.js:136-158` создаёт rateLimit для auth/invite/admin без store. В `server/vercel.json` backend упакован как serverless Node. Поиск productionserver не нашёл store/MemoryStore/custom limiter. [Документация express-rate-limit](https://express-rate-limit.mintlify.app/reference/stores) прямо определяет default memory-store как независимый для каждого процесса и сбрасываемый при restart.

Сценарий: два или более Vercel instances обслуживают один IP. Каждый разрешает свой набор20auth/min или5admin/15мин; новые/cold instances начинают с нуля. Объявленный общий предел не выдерживается, brute-force/abuse budget возрастает. На единственном непрерывно работающем процессе ограничение работает; число реальных productioninstances и фактический обход не измерены. B04 alias bypass — отдельный router дефект, это исправление его не закрывает.

Минимум: общий поддерживаемый store для всех replicas и корректный namespace, явная обработка отказа store (0.5дня; + единый счётчик, − внешняя зависимость/latency). Правильно: комбинированные IP+account/device limits, общий store/edge enforcement, budget тяжёлых операций и мониторинг отказов без раскрытия credentials (1–2дня; + защита границ API при масштабировании, − больше конфигурации и операционного контроля). Не включать этот условный finding в число воспроизведённых production incidents.

## Сквозной поиск: что сделано

- Два прохода `rg` по всемуworkspace first-party тексту, включая hidden: обычный tracked/неignored, затем --no-ignore для локальных конфигураций. Исключены node_modules, Pods, build/dist, .git objects, .expo, audit, assets/xcassets/screens и бинарныеDB, lockфайлы проверяет отдельный dependency проход. Паттерны private key headers, API/token prefixes, SECRET/TOKEN/PASSWORD/API_KEY/PRIVATE_KEY/DATABASE_URL assignments. Вывод сначала file names, затем только matched names/prefix metadata, без значений credentials.
- `git ls-files '*env*' '*key*' '*credential*' '*.pem' '*.p12' '*.jks'` нашёл tracked env examples/config loader; tracked live.env/privatekeyfile в этих шаблонах не найден. Всего381 trackedfile на текущемHEAD. `server/.env` игнорируется repo `.gitignore:26`; `.claude/settings.local.json` игнорируется global git ignore. Их наличие само по себе ожидаемо и не объявлено публикациейсекретов; локальные сохранённые команды/token assignments не копировались в отчёт, реальность/действительность значений не проверялась.
- Совпадения first-party production представлены env lookups, dev fallback, OAuth public client IDs, test constants и намеренным demo seed. Живой опубликованный секрет данным проходом не подтверждён; это не гарантия отсутствия секретов, особенно в истории/бинарниках. Содержимое всей gitистории и удалённых deployment env не сканировалось.
- Отдельный global search опасных вызовов eval/new Function/child_process/execSync/spawn/readFile/writeFile/unlink/innerHTML/dangerouslySetInnerHTML/multer/sendFile/download/SQLquery. Runtime dynamic evaluation найден только в htmlEscape unit test, не в request path. File reads — миграционный исполнитель по локальному списку файлов и test fixture; untrusted upload/filepath endpoint не найден. Существующий admin innerHTML sink подробноH01, не новый count. Public invitejsonForScript проверенH и безопасно кодирует script context.
- Поиск SQL interpolation `${...}` в routes/services: найдены placeholders на длину массивов и whitelist колонок Auth, а значения передаются параметрами; input-derived raw SQL value interpolation в прочитанных request paths не обнаружена. Это статическое заключение по данным paths, не результат fuzzing/SAST.
- Server CORS allowlist BASE_URL+localhost, запросы без Origin разрешены для native; bearer auth без auto-cookie, поэтому самостоятельный CSRF на этих request paths не подтверждён. CORS сам по себе не авторизация; requireAuth/ownership аудированыA–G. AdminXSSH01 использует действующую bearer сессию и не лечится CORS. Prod headers globalHelmet, adminrelaxed, invite mismatchH02. Trustproxy=1 зависит от размещения за одним доверенным proxy; прямой интернет-доступ Node/реальные proxyheaders не проверялись.

## Логи — дополнение к AC04, не отдельный count

Прямые `console.log` в `src/shared/services/notifications.ts:66`, `useNotifications.ts:36,47,75` выводят token/notification payload безproductionguard. Ошибки register/logout Axios передаются целиком в console (`notifications.ts:92,159`); AC04 уже содержит аналогичный OAuth/session sink. Server `pushNotificationService.js:127` логирует token через logger.info, **который выключен в production** (`server/utils/logger.js:26-29`), поэтому нельзя приписывать этой строке unconditionalprodleak. Serverlogger error42-46 при Error выводит message, stack только nonprod; clientloggerwarn/error не редактирует args. Фактическая отправка этих логов во внешний collector/доступ сторонних лиц не подтверждены. Рекомендации AC04 расширить единым redact/error DTO и удалением прямых consoletokenpayload.

Первый широкий logging output был усечён; calendar/export directconsole отдельно перечитан, остальные servicefiles полностью закрытыE/G. Не считать усечённый вывод полным перечнем всехlogcalls.

## Документы и ограничения

Полностью прочитаны CLAUDE.md886 (envexampleсекция755–791 с redacted values), docs/README40, quick-reference42, api-standards195, app-store-release208, known-issues230, scripts/check-secrets.sh126, server/clientlogger. Новые CLAUDE additions193–391 совпадают с обнаруженными C/D/E invariants; утверждение о конкретных3секундах DoS — чужое описание, аудит не замерял и не выдаёт его за свой результат.

Документы не являются доказательством текущего поведения: «XSS закрыты экранированием» app-store-release200 противH01; «failed push releasesclaim» CLAUDE противGR01/02; «both suites green650/47» — историческое заявление, ниодногозапускаэтогоаудитанебыло. app-store-release устарел оdisablednotificationplugin, ESLint9broken и oldsecretselfmatch; currentconfigs проверяетдругойIпроход. Старыеapiдокументы1780 строк отдельно читаетдругойаудиторI.

Намеренное ограничение из known-issues: календарь устройства обновляется только при работе/foreground приложения; silent background push решили не делать. Это product limitation/manualcheck, не объявлять новым unattendedcalendarbug поверхE. Push backstack изknown-issues остаётся reported/manual, не статическидоказанная новаянаходка. Старые candidatelists не импортировались в findings без проверки; многие уже закрыты либо объединеныA–H.

Прочие ограничения безновогоID: bug-report write не имеет rate/quota/maxlength сверх globalexpressJSONcap, расширение общего abusebudget рекомендуется в IS02; globalerrorhandler `server.js:362` отдаёт String(err) вопрекиCLAUDE обещанию generic errors, но stack/чужиеcredentials в достижимомответе не подтверждены. Это не новые Critical vulnerabilities.
