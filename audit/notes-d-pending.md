# Участок D — сохранённые результаты независимого чтения

2026-09-07. Проверяющие forms/planner/display завершили чтение production, но были прерваны лимитом до записи заметок. Сюда сразу перенесены их сообщения; все неподтверждённые стыки далее сверяет главный аудитор. Это рабочие заметки, не окончательное включение findings.

- Forms: production calendar hooks и AddRehearsalScreen прочитаны полностью. Кандидаты: participant_ids=[] превращается в undefined, сервер PUT сохраняет старых participants; default start23,endnextday01 сравнивается как Date корректно, но save сериализует обе границы на одинаковую дату; edit использует device clock вместо user TZ; multi-create partial failures/retry. title/location undefined на сервере очищаются — не finding.
- Planner: все production smart-planner и conflictDetection прочитаны, 2153 строки вместе с тремя тестами (степень завершённости tests уточнить). Гонка смены проекта SmartPlannerScreen144-154 запоминает old simpleMembers как initialized newProjectId. Header date-only local getDate сдвигает UTC−. Screen397-402 не передаёт selectedMemberIds в форму. conflictDetection56-59 игнорирует tentative, merger58 считает busy; active caller Submit183. Tab22 выбирает projects[0] без is_admin, selector204 только admin. Последний пункт требует навигации H.
- Display: все production CalendarScreen/RehearsalDetailsScreen/components/formatters + SeenContext прочитаны. Forced reload on filter CalendarScreen112-115 есть (не считать TTL сам по себе bug). Details68-81 loadById, legacy поля сервер возвращает (undefined-проблема опровергнута). Details не prime SeenContext, toggle использует stale/null response; local participants143-149 optimistic не откатываются при caught failure, stats152 старое closure. CalendarScreen185 и WeeklyCalendar59-60 считают только стартовый день; overnight/multiday второй день пуст. Today WeeklyCalendar133-140 только scroll, не onDaySelect. Hooks error=>available проверить отдельно.
