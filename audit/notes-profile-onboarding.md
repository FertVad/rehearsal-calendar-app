# H — профиль и онбординг (клиент)

Статус: H в этой подзадаче завершён. Только статический read-only аудит; приложение, тесты, сборки и другие скрипты не запускались. Путь источников относительно `rehearsal-calendar-native/`.

## HP01 — Medium: изменение email подтверждается, но сервер его игнорирует

- Доказательство: `src/features/profile/screens/EditProfileScreen.tsx:66–73`: `await updateUser({ firstName: firstName.trim(), lastName: lastName.trim() || undefined, email: email.trim() });` затем `Alert.alert(t.profile.profileUpdated, ...)`. В `server/routes/auth.js:301–323` whitelist содержит имя/фамилию и настройки, **email отсутствует**; цикл `:334–337` обрабатывает только whitelist. Поля имени всегда отправляются, поэтому запрос не попадает в `No fields to update`.
- Сценарий: пользователь меняет только email на валидный новый, нажимает Save, получает «профиль обновлён»; ответ и повторное открытие показывают прежний email. Новым адресом входить нельзя. Для аккаунта без email обязательная клиентская проверка `EditProfileScreen.tsx:47–50` дополнительно мешает сохранять имя без ввода адреса, который всё равно не сохранится.
- Минимально: убрать редактирование email и валидацию неизменяемого адреса из общей формы (плюс: честный UI и небольшой объём; минус: смена email остаётся недоступна; 1–2 ч).
- Правильно: отдельный проверяемый сценарий смены email с нормализацией, уникальностью, подтверждением адреса/учётных данных и явным состоянием ожидания (плюс: полноценная безопасная функция; минус: сервер, почтовая доставка и состояния UI; 1–3 дня). Не исправлять одним добавлением поля в whitelist.

## HP02 — Low: существующую фамилию нельзя очистить

- Доказательство: `src/features/profile/screens/EditProfileScreen.tsx:68`: `lastName: lastName.trim() || undefined`; `server/routes/auth.js:335–337`: `const value = req.body[apiField]; if (value === undefined) continue;`. JSON не передаёт undefined, поэтому пустая фамилия означает отсутствие изменения. После запроса UI сообщает успех `EditProfileScreen.tsx:73`.
- Сценарий: стереть прежнюю фамилию целиком и сохранить; сервер оставляет прежнюю фамилию, она снова видна после возврата в профиль. Простая замена фамилии работает.
- Минимально: передавать пустую строку как явное очищение (плюс: узкий фикс, текущий trimName поддерживает строку; минус: нужно согласовать представление пустоты; 0.5–1 ч).
- Правильно: закрепить patch-контракт `undefined = не изменять`, `null/пустая строка = очистить`, привести типы/сервер и проверить очистку отдельно (плюс: однозначное поведение всех optional полей; минус: небольшой совместный API/UI объём; 2–4 ч).

## HP03 — Low: ошибка «пропустить онбординг» остаётся необработанным Promise

- Доказательство: `src/features/onboarding/hooks/useOnboarding.ts:18–20`: catch пишет в console и `throw error`; callback системного Alert `:34–35`: `onPress: async () => { await completeOnboarding(); }` без catch. Обычное завершение в `WeekStartScreen.tsx:31–41` в отличие от skip имеет обработку и Alert.
- Сценарий: без сети нажать Skip и подтвердить; обновление `onboardingCompleted` отклоняется, ошибка уходит из callback Alert без пользовательского сообщения. Пользователь остаётся в онбординге без объяснения; это не утверждение о native crash.
- Минимально: catch с локализованным Alert в callback Skip (плюс: ясный отказ; минус: остаётся раздельная обработка завершения; 0.5–1 ч).
- Правильно: единая операция завершения со статусом/ошибкой, защитой повторного отправления и одинаковым UI для Finish/Skip (плюс: согласованность; минус: изменение контракта hook/экранов; 2–4 ч).

## HP04 — Medium: ручная настройка timezone сбрасывается при следующем запуске/входе

- Доказательство: `src/features/profile/screens/ProfileScreen.tsx:92–97`: `await updateUser({ timezone });` с успешным закрытием модалки; выбор зоны доступен `:261–274`. `src/contexts/AuthContext.tsx:166–191` при login/restore один раз за сессию получает `deviceTimezone`; если отличается от `user.timezone`, безусловно выполняет `authAPI.updateMe({ timezone: deviceTimezone })` (`:176–180`). Флага ручного выбора/автоматического режима нет.
- Сценарий: на устройстве с зоной A выбрать в профиле зону B для работы с расписанием, получить успешное сохранение, перезапустить приложение с сетью. Restore молча переписывает серверную настройку обратно на A. Это подтверждённая потеря явной настройки; сами ISO timestamps не изменяются, но время в интерфейсах/API, использующих user timezone, меняет представление. Отдельно от AC03 late-session response: здесь не требуется гонка или смена аккаунта.
- Минимально: автоматически заполнять только отсутствующую timezone (плюс: ручной выбор сохраняется; минус: при поездках не следует за устройством; 1–2 ч).
- Правильно: настройка «автоматически по устройству / выбранная зона» с сохранённым режимом, auto-sync только в первом режиме (плюс: ясное поведение для поездок и удалённых проектов; минус: изменение API/настройки UI; 0.5–1 день).

## HP05 — Low: онбординг показывает подключённый календарь после отказа сохранения

- Доказательство: `src/features/onboarding/screens/CalendarSyncScreen.tsx:130–141` сначала `setSelectedCalendarId(calendarId)`, затем `await updateSettings(...)`; catch `:144–148` показывает ошибку, но не откатывает selection. Зелёная галочка `:261–262` и итоговый текст «Занятые часы будут подтягиваться сами» `:295–297` выбираются только по selectedCalendarId. `useCalendarSync.ts:149–156` и `calendarStorage.ts:201–208` пробрасывают отказ AsyncStorage, поэтому это достижимая ветка, а не обработка недостижимого исключения.
- Сценарий: native storage отказывает при выборе календаря; закрыть Alert об ошибке. Визуально календарь по-прежнему подключён, Next снова доступен, хотя settings не сохранены и выбранный sync работать не будет. Другой обычный стык: при повторном входе в этот шаг локальный selectedCalendarId снова null, хотя ранее сохранённый settings может быть включён — UI вообще не производит свой статус из сохранённых настроек.
- Минимально: выставлять selection после успешного updateSettings либо откатывать к прежнему значению в catch (плюс: правильный статус после отказа; минус: повторный mount требует отдельной инициализации; 1–2 ч).
- Правильно: различать draft/saving/saved и выводить итог подключения из актуального settings; инициализировать выбор после загрузки (плюс: честные состояния и повторный вход; минус: несколько состояний экрана; 3–5 ч).

## Проверенные стыки и покрытие

- Удаление аккаунта: текущий UI заранее предупреждает о **всех принадлежащих пользователю проектах и репетициях**, включая потерю для остальных участников. `ProfileScreen.tsx:525` показывает `t.profile.deleteAccountWarning`; тексты `src/i18n/translations/profile.ts:64,114,164,214` однозначны. Расхождение privacy/support «только единственный администратор» передано root, отдельной ошибки подтверждения UI здесь нет.
- Полностью прочитаны: `ProfileScreen.tsx` (556), `EditProfileScreen.tsx` (206); `WelcomeScreen.tsx` (128), `WeekStartScreen.tsx` (155), `CalendarSyncScreen.tsx` (467); `OnboardingStep.tsx` (197), `OnboardingProgress.tsx` (47), `WeekPreview.tsx` (59), `OnboardingNavigator.tsx` (34); `useOnboarding.ts` (43, ранее G + точечная сверка сейчас).
- `NotificationsScreen.tsx` полностью покрыт G, повторно не читался; `CalendarSyncSettingsScreen.tsx` покрыт E, не перечитывался. Profile notification registration issues уже GC01–03; гонки сессии AuthContext уже AC03. Сценарии не считать повторно.
- Полностью прочитаны также четыре barrel-файла `src/features/onboarding/index.ts`, `screens/index.ts`, `components/index.ts`, `hooks/index.ts`; весь production в назначенном подмножестве покрыт, кроме явно выделенных в E/G файлов. Navigation-стык: `OnboardingNavigator.tsx:10–15,28–31` содержит только Welcome/CalendarSync/Notifications/WeekStart, без JoinProject; передано автору HN01.
- Язык: `ProfileScreen.tsx:80–85` сначала меняет I18nContext/local storage, затем сервер; при отказе сервера rollback отсутствует. После повторного открытия того же языка `:77` early return не позволяет повторно сохранить его на сервер. Передано общему i18n-аудитору как стык HC01; здесь не отдельный count.
- Низкоприоритетный UI-дефект без отдельного count: `ProfileScreen.tsx:320–328` показывает Help со стрелкой, но TouchableOpacity не имеет onPress; необходим реальный support link/экран или удаление интерактивного оформления (0.5–1 ч). `:317` версия hardcoded; конфигурацию I не читали, несоответствие реальной версии не утверждается.
- Локали: прочитаны нужные excerpts onboarding/profile для фактических сообщений и предупреждения удаления. Отдельные файлы profile styles не входили в этот проход, inline styles перечисленных экранов прочитаны.
- Тесты: поиск в src и по именам файлов всего проекта не нашёл тестов Profile/EditProfile/onboarding/useOnboarding; ранее A полностью прочитан `src/__tests__/integration/authFlow.test.ts`, это не тест этих экранов. Покрытие тестов данной подзадачи ограничено поиском и существующим A; тесты не запускались. Стыки server whitelist/AuthContext/calendar settings прочитаны адресно, полное их покрытие относится к A/E, а не повторно к H.
